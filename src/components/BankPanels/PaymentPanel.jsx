import React from 'react'
import { withStyles } from '@material-ui/core'
import { styles } from './styles'


export const PaymentPanel = withStyles(styles)(({ classes }) => {
        return (
                <div>

                </div>
        )
})
