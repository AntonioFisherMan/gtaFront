import { createStyles } from "@material-ui/core";

export const styles = () => createStyles({
        input: { color: 'white', fontSize: 13, },
        label: {
                fontWeight: '400',
                fontSize: 15,
                marginLeft: 5,
                borderBottom: 'border: 1.5px solid rgba(255, 255, 255, 0.5)',
                color: '#ffffff',

        }
})